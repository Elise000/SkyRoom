<br>
<br>

<div class="container">
	<h1><%= @user.username %> PROFILE</h1>
	<% unless @user.avatar_url.nil? %><%= image_tag @user.avatar_url(:display) %><br/><% end %>

	<%= link_to "My listing", listings_path %>
	<button> My bookings </button>
<div class="container">

<!--
<script>
	var url = '<%= user_url(@user) %>'
  if(window.opener) {
   
    window.close()

    window.opener.location= url;
  }
</script>